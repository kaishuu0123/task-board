<%= stylesheet_pack_tag 'bootstrap' %>
<div class="col-md-6">
  <h1>Login</h1>
  <%= form_for((@user), url: finish_signup_path(@user)) do |f|%>
    <%= f.text_field :username %>
    <%= f.text_field :email %>
    <%= f.submit 'Add email', class: 'btn btn-primary' %>
  <% end %>
</div>